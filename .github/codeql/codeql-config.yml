name: "Java Security Scan"

# 必须配置的查询部分
queries:
  - uses: security-and-quality  # 必须包含至少一个uses
  - uses: security-extended     # 质量与安全扩展规则
  - uses: security-experimental # 实验性规则（可选）

# 排除路径（按需修改）
paths-ignore:
  - "**/test/**"
  - "**/generated/**"